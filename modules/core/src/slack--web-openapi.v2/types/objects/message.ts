import { UserID, ChannelID, TS } from "../definitions";
import { intersection, type, partial, Int, boolean } from "io-ts";

export const Message = intersection([
  type({

  }),
  partial({

  })
]);
